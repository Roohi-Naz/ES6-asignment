// mathOperations.js
export function operation(a, b) {
    let add = a + b;
    let subt = a - b;
    let multiply = a * b;
    let divide = a / b;
    console.log(`Add: ${add}, Subtract: ${subt}, Multiply: ${multiply}, Divide: ${divide}`);
}

